import React, { useState } from "react";
import ResultsView from "./ResultsView";

const Results = ({ groupId }) => {
  const handleWishListSubmit = () => {};
  return (
    <ResultsView user={{}} status={{ inProgress: true }} isFetching={true} />
  );
};

export default Results;
