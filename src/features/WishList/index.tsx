import React, { useState } from "react";
import WishListView from "./WishListView";

const WishList = () => {
  const handleWishListSubmit = () => {};
  return <WishListView onSubmit={handleWishListSubmit} />;
};

export default WishList;
